interface TasksProps {
    data: {
        
    }
}

export default function Tasks(props: TasksProps) {
    // Scrape JIRA for task information, completed and committed, to calculate histogram of tasks and throughput.
    // PRODUCT: estimated completion times on active tasks
    // PRODUCT: show IC affinity towards a given ticket based on historical data.

    return (
        <h1>Tasks Tab</h1>
    )
}