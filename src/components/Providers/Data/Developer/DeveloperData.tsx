import { PropsWithChildren, createContext, useContext, useEffect } from "react";

const DeveloperDataContxt = createContext(null);

export default function DeveloperData(props: PropsWithChildren) {
    useEffect(() => {

    });



    return (props.children)
}